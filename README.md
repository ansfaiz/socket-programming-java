# Java Socket Web Server

This repo contains simple client-server programs written in Java.

## Structure
- `singleThread/` → single-threaded client/server demo
- `multiThread/` → multi-threaded client/server demo

## How to Run
1. Compile:
   ```bash
   javac Server.java
   javac Client.java

